# 南昌大学竞赛队友匹配平台 - 项目交付文档

---

## 一、项目启动文档

### 1.1 环境要求

| 组件 | 版本要求 | 说明 |
|------|---------|------|
| JDK | 17 或以上 | 后端运行环境 |
| Maven | 3.6+ | 后端构建工具 |
| MySQL | 8.0 | 数据库 |
| Redis | 6.0+ | 缓存服务 |
| Node.js | 18+ | 前端运行环境 |
| npm | 9+（随Node.js安装） | 前端包管理 |

### 1.2 后端启动步骤

**第一步：启动 MySQL 和 Redis**
```bash
mysql --version
redis-cli ping    # 应返回 PONG
```

**第二步：初始化数据库**
```bash
mysql -u root -p --default-character-set=utf8mb4 < backend/src/main/resources/db/schema.sql
```
脚本自动创建 `match_platform` 数据库、7张表、预置标签和测试用户。

**第三步：修改数据库密码（如需要）**
编辑 `backend/src/main/resources/application.yml`，修改 `spring.datasource.password` 和 Redis 相关配置。

**第四步：启动后端**
```bash
cd backend
mvn clean package -DskipTests
mvn spring-boot:run
```
后端启动成功标志：控制台输出 `Started MatchApplication`，监听端口 8080。

### 1.3 前端启动步骤

```bash
cd frontend
npm install
npm run dev
```
访问 `http://localhost:3000` 即可看到登录页面。

---

## 二、测试账号

### 2.1 管理员账号

| 学号 | 姓名 | 密码 | 角色 |
|------|------|------|------|
| **root** | **管理员** | **123456** | **ADMIN（管理员）** |

> 管理员登录后可见"管理后台"菜单，可管理竞赛类别、用户、招募。

### 2.2 普通用户账号

密码均为 **123456**：

| 学号 | 姓名 | 核心技能标签 | 简介 |
|------|------|-------------|------|
| 8008123001 | 张三 | Java(精)/算法(精)/后端(精)/Python(掌)/ML(了) | 计算机大三，热爱算法和数学建模 |
| 8008123002 | 李四 | Python(精)/ML(精)/数据(掌)/论文(掌)/前端(了) | 数据科学专业，擅长Python |
| 8008123003 | 王五 | 嵌入式(精)/单片机(精)/C++(掌)/后端(了) | 电子信息工程大二，嵌入式开发 |
| 8008123004 | 赵六 | 前端(精)/UI(精)/Python(了)/项目管理(了) | 数字媒体专业，UI设计 |
| 8008123005 | 孙七 | 算法(精)/MATLAB(精)/Python(掌)/数据(掌)/论文(掌) | 数学系研一，数学建模 |
| 8008123006 | 周八 | ML(精)/Python(精)/数据(掌)/算法(掌) | 人工智能大三，深度学习 |
| 8008123007 | 吴九 | Java(精)/后端(精)/前端(掌)/项目管理(掌)/Python(掌) | 软件工程大三，全栈开发 |
| 8008123008 | 郑十 | 数据(精)/Python(精)/ML(掌)/论文(了) | 大数据大二，数据可视化 |
| 8008123009 | 陈小明 | FPGA(精)/嵌入式(精)/单片机(掌)/C++(了) | 电子科学大三，FPGA方向 |
| 8008123010 | 刘小红 | 论文(精)/项目管理(掌)/MATLAB(了)/UI(了) | 信息管理大二，文档撰写 |
| 8008123011 | 黄小刚 | C++(精)/算法(精)/Java(掌)/Python(掌) | 计算机大二，ACM集训队 |
| 8008123012 | 林小美 | 项目管理(精)/UI(掌)/前端(掌)/论文(了) | 工商管理大三，团队管理 |

> 系统共有 11 条招募信息，覆盖数学建模、程序设计、电子设计、创新创业、挑战杯等类别。15 条申请记录，其中多条已通过（ACCEPTED），形成了真实的队友关系。

---

## 三、Redis 配置与缓存说明

### 3.1 配置文件
```yaml
spring:
  redis:
    host: localhost
    port: 6379
    timeout: 3000ms
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
```

### 3.2 缓存应用

**缓存1：技能标签列表缓存**
- 键名：`tags:all`，TTL：30 分钟
- 实现：`TagService.listAllTags()` — 先查 Redis，未命中查 MySQL 并回写缓存
- 好处：标签数据变动少但查询频繁，缓存命中率高

**缓存2：推荐队友结果缓存**
- 键名：`recommendations:recruitment:{recruitmentId}`，TTL：30 分钟
- 实现：`MatchingService.getRecommendations()` — 首次执行匹配算法后缓存结果
- 失效策略：新申请提交或招募关闭时主动调用 `evictCache()` 清除
- 好处：匹配算法需遍历所有用户计算交集，计算量随用户增长，缓存大幅降低重复计算

**序列化配置：** Key 用 `StringRedisSerializer`，Value 用 `GenericJackson2JsonRedisSerializer`

---

## 四、操作演示步骤（答辩用）

**步骤1：注册新用户**
1. 访问 `http://localhost:3000`，点击"立即注册"
2. 输入学号、姓名、密码、手机号、QQ号
3. 点击"注册"，跳转登录页

**步骤2：登录**
1. 使用测试账号登录（如 `8008123001` / `123456`）
2. 登录后进入招募广场

**步骤3：设置技能标签**
1. 点击"个人中心" → "技能标签"标签页
2. 从下拉框选择技能和熟练度，点击"添加"
3. 可随时删除标签

**步骤4：浏览招募广场**
1. 查看招募卡片（标题、类别、需人数、状态）
2. 使用搜索框和类别筛选器查找

**步骤5：发布招募**
1. 点击"发布招募"，填写标题、类别、人数、技能要求、描述
2. 发布后出现在广场列表中

**步骤6：查看详情与推荐队友**
1. 点击招募卡片进入详情页
2. 查看推荐队友区域——系统自动按技能匹配度计算 Top 5
3. 匹配度 ≥ 70% 绿色，40%-70% 橙色，< 40% 灰色

**步骤7：申请加入**
1. 点击"申请加入"，填写申请理由
2. 系统校验不重复申请

**步骤8：审核申请（发布者操作）**
1. 发布者登录，进入招募详情，点击"查看申请列表"
2. 对申请点击"同意"或"拒绝"
3. 同意后，双方可在"个人中心 → 我的队友"中互见联系方式

**步骤9：管理员操作**
1. 使用 `root` / `123456` 登录
2. 导航栏出现"管理后台"菜单
3. 竞赛类别管理：添加/删除类别
4. 用户管理：查看所有用户、删除用户（管理员不可删）
5. 招募管理：查看所有招募、关闭招募、删除招募

---

## 五、匹配推荐算法说明

### 5.1 算法名称
基于技能标签交集的加权匹配评分算法（Tag Intersection Weighted Matching）

### 5.2 算法公式
```
对于每个候选用户 u（排除发布者本人）：

1. 交集标签集合 = 招募要求标签集合 ∩ 用户技能标签集合
2. 用户总得分 = Σ（交集内每个标签的用户熟练度分数）
   熟练度分数映射：了解=1, 掌握=2, 精通=3
3. 匹配度百分比 = 用户总得分 / (要求标签数 × 3) × 100%
4. 按匹配度百分比降序排列，取前 5 名
```

### 5.3 计算示例
招募要求：[算法设计, Python, 论文写作]，满分 = 3 × 3 = 9

用户李四的技能：
- Python(精通=3) → 匹配，贡献 3
- 论文写作(掌握=2) → 匹配，贡献 2
- 其余不匹配

李四总得分 = 5，匹配度 = 5/9 × 100% = 55.6%

### 5.4 代码位置
`backend/src/main/java/com/matchteam/service/MatchingService.java`

---

## 六、技术栈说明

### 6.1 后端

| 技术 | 版本 | 作用 |
|------|------|------|
| Java | 17 | 编程语言（LTS） |
| Spring Boot | 3.0.12 | 应用框架，自动配置、内嵌Tomcat |
| Spring Security | 内嵌 | 安全框架，BCrypt加密、JWT过滤器链、角色鉴权 |
| JWT (jjwt) | 0.12.3 | 无状态认证，token含用户ID和角色 |
| MyBatis Plus | 3.5.5 | ORM框架，LambdaQueryWrapper、分页插件 |
| MySQL | 8.0 | 关系型数据库 |
| Redis | 6.0+ | 缓存标签列表和推荐结果 |
| Maven | 3.6+ | 项目构建与依赖管理 |
| Lombok | 内嵌 | 自动生成getter/setter/构造器 |
| Validation | 内嵌 | 声明式参数校验 |

### 6.2 前端

| 技术 | 版本 | 作用 |
|------|------|------|
| Vue 3 | 3.4+ | 前端框架，Composition API |
| Vite | 5.0+ | 构建工具，ESM开发服务器 |
| Element Plus | 2.5+ | UI组件库 |
| Vue Router | 4.3+ | SPA路由，含角色守卫 |
| Axios | 1.6+ | HTTP客户端，拦截器自动附加JWT |
| @element-plus/icons-vue | 2.3+ | 图标库 |

---

## 七、UML 素材

### 7.1 系统用例描述

#### UC-01：用户注册
- 参与者：未注册用户
- 前置条件：访问注册页面
- 基本事件流：输入学号/姓名/密码等 → 校验唯一性 → BCrypt加密存储 → 返回成功

#### UC-02：用户登录
- 参与者：已注册用户
- 前置条件：已有账号
- 基本事件流：输入学号密码 → BCrypt验证 → 生成JWT（含userId+role） → 前端存储token/role

#### UC-03：管理个人技能标签
- 参与者：已登录用户
- 基本事件流：进入个人中心 → 选择标签+熟练度 → 不重复校验 → 保存关联

#### UC-04：发布竞赛招募
- 参与者：已登录用户
- 基本事件流：点击发布 → 填写标题/类别/人数/技能/描述 → 创建招募+标签关联

#### UC-05：浏览招募广场
- 参与者：已登录用户
- 基本事件流：进入广场 → 分页查询 → 关键词搜索 → 类别筛选

#### UC-06：查看招募详情与推荐队友
- 参与者：已登录用户
- 基本事件流：点击卡片 → 显示详情 → 匹配算法计算Top5 → Redis缓存推荐结果

#### UC-07：申请加入招募
- 参与者：已登录用户（非发布者）
- 基本事件流：点击申请 → 填写理由 → 校验不重复 → 创建PENDING申请 → 清除推荐缓存

#### UC-08：审核申请
- 参与者：招募发布者
- 基本事件流：查看申请列表 → 同意/拒绝 → 双方在"我的队友"互见联系方式

#### UC-09：查看我的队友
- 参与者：已登录用户
- 基本事件流：进入个人中心 → 查询ACCEPTED申请 → 显示队友信息+联系方式

#### UC-10：管理员管理平台
- 参与者：管理员（ADMIN角色）
- 基本事件流：登录 → 管理后台 → 管理竞赛类别/用户(增删)/招募(关闭删除)
- 权限控制：非ADMIN用户访问返回403

### 7.2 主要类图说明

```
后端分层架构：

Controller层（6个）：
  AuthController / UserController / TagController
  RecruitmentController / ApplicationController / AdminController
  依赖 ↓
Service层（7个）：
  UserService(注册/登录)  TagService(标签CRUD+缓存)
  CategoryService  RecruitmentService  MatchingService(算法+缓存)
  ApplicationService  TeammateService
  依赖 ↓
Mapper层（7个，继承MyBatisPlus BaseMapper）：
  UserMapper  TagMapper  UserTagMapper  CategoryMapper
  RecruitmentMapper  RequiredTagMapper  ApplicationMapper
  操作 ↓
Entity层（7个实体，映射数据库表）：
  User(含role字段)  Tag  UserTag  CompetitionCategory
  Recruitment  RecruitmentRequiredTag  Application

Common层：Result<T> / JwtUtils / SecurityUtils / BusinessException / GlobalExceptionHandler
Config层：SecurityConfig(含角色鉴权) / JwtAuthenticationFilter / RedisConfig / CorsConfig / MyBatisPlusConfig
```

### 7.3 包图说明

**后端包结构（com.matchteam）：**
```
com.matchteam
├── entity/      7个实体类
├── mapper/      7个Mapper接口
├── service/     7个Service类
├── controller/  6个Controller
├── dto/         14个DTO
├── common/      Result, JwtUtils, SecurityUtils, Exception, Handler
└── config/      Security, JWT Filter, Redis, CORS, MyBatisPlus
```

**前端包结构（src/）：**
```
src/
├── main.js / App.vue
├── router/index.js
├── api/       request.js, auth.js, user.js, tag.js, recruitment.js, admin.js
├── views/     LoginView, RegisterView, SquareView, DetailView, ProfileView, AdminView
└── components/  NavBar.vue
```

### 7.4 部署图说明

```
浏览器(localhost:3000) → Vite Dev Server(:3000) → Spring Boot(:8080)
                                                    ├── MySQL(:3306)
                                                    └── Redis(:6379)
```
Vite 代理 `/api` 到后端 8080；后端通过 JDBC/Lettuce 连接 MySQL/Redis。

---

## 八、数据库设计

### 8.1 表结构（7 张表）

| 表名 | 说明 | 核心字段 |
|------|------|---------|
| `user` | 用户表 | id, student_id, name, password(BCrypt), phone, qq, intro, **role(VARCHAR, USER/ADMIN)**, create_time |
| `tag` | 技能标签表 | id, name |
| `user_tag` | 用户-标签关联 | id, user_id, tag_id, proficiency(1=了解/2=掌握/3=精通) |
| `competition_category` | 竞赛类别表 | id, name |
| `recruitment` | 招募信息表 | id, publisher_id, title, category_id, required_number, description, status(OPEN/CLOSED), create_time |
| `recruitment_required_tag` | 招募-所需标签 | id, recruitment_id, tag_id |
| `application` | 申请表 | id, recruitment_id, applicant_id, reason, status(PENDING/ACCEPTED/REJECTED), apply_time |

### 8.2 API 端点一览

**认证（/api/auth）：**
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/auth/register | 注册 |
| POST | /api/auth/login | 登录（返回token+userId+name+studentId+role） |

**管理员（/api/admin，需ADMIN角色）：**
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/admin/categories | 类别列表 |
| POST | /api/admin/categories | 添加类别 |
| DELETE | /api/admin/categories/{id} | 删除类别 |
| GET | /api/admin/users | 用户列表 |
| DELETE | /api/admin/users/{id} | 删除用户（不可删自己/管理员） |
| GET | /api/admin/recruitments | 招募列表 |
| PUT | /api/admin/recruitments/{id}/close | 关闭招募 |
| DELETE | /api/admin/recruitments/{id} | 删除招募 |

**用户/标签/招募/申请（/api，需认证）：** 略（共约20个端点）

---

## 九、项目文件清单

### 后端（54个Java文件 + 3个资源文件）
- `pom.xml` + `application.yml` + `schema.sql`
- entity × 7, mapper × 7, service × 7, controller × 6, dto × 14, common × 5, config × 5

### 前端（17个源文件 + 配置）
- `package.json` + `vite.config.js` + `index.html`
- router × 1, api × 6, views × 6, components × 1, App.vue, main.js

---

*文档更新日期：2026年5月13日*
